# Causal Mediation Release Outputs

This folder contains aggregate, public-safe causal mediation outputs from the
strict joint-history mediation workflow.

Only pooled or otherwise aggregate outputs should be staged here. Do not include
subject-level inputs, imputation objects, fitted model objects, or
imputation-specific result files unless a separate disclosure review approves
them.
